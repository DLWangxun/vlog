eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9999/eureka
management:
  security:
    enabled: false
spring:
  cloud:
    bus:
      trace:
        enabled: true
    config:
      discovery:
        enabled: true
        serviceId: vlog-config-server
      label: master
      name: message-producer
      profile: default

